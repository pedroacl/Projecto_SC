package network.messages;

public enum MessageType {
	OK, NOK, MESSAGE, FILE, ADDUSER, REMOVEUSER, RECEIVER, CONVERSATION, LAST_MESSAGES, CONTACT, KEY, PGP_MESSAGE, AUTH;
	//CONTACT_TYPE: user ou grupo
}

